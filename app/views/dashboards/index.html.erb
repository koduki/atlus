<nav>
  <ul>
  <% @dashboards.each do |dashboard| %>
    <li>
      <span><%= link_to dashboard.name, dashboard %></span>
      <span><%= link_to 'Edit', edit_dashboard_path(dashboard) %></span>
      <span><%= link_to 'Destroy', dashboard, confirm: 'Are you sure?', method: :delete %></span>
    </li>
  <% end %>
  </ul>
<nav>
<%= link_to 'New Dashboard', new_dashboard_path %>
